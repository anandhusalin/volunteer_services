import 'package:flutter/cupertino.dart';

class ListItem{
  String tiitle;
    IconData icon;
    bool isSelected;
    ListItem({
    required this.icon,
      required this.tiitle,
      required this.isSelected

});
}